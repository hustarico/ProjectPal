package com._4.ProjectPal.notification;

public enum NotificationType {
    PROJECT_INVITATION,
    TASK_ASSIGNED,
    DEADLINE_REMINDER,
    JOIN_REQUEST,
    JOIN_APPROVED,
    JOIN_REJECTED,
    NEW_MESSAGE
}